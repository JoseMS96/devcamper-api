{
  "name": "devcamper_api",
  "version": "1.0.0",
  "description": "Projeto sobre sistema de gerenciamento de bootcamps com cursos, tem authentication, session, password recovery, senhas encriptadas, calcula todos bootcamps dentro de um raio, etc",
  "main": "server.js",
  "scripts": {
    "start": "SET NODE_ENV=production && node server",
    "dev": "nodemon server"
  },
  "author": "Jose Melo",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
